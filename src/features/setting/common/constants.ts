import { EMPTY } from "@/constants/common";

import { SettingFormValues } from "../schema";

export const settingDefaultValues: SettingFormValues = {
  appName: EMPTY.str,
  appDescription: EMPTY.str,
  logoUrl: EMPTY.str,
  faviconUrl: EMPTY.str,
  primaryColor: EMPTY.str,
  email: EMPTY.str,
  phone: EMPTY.str,
  address: EMPTY.str,
  facebook: EMPTY.str,
  twitter: EMPTY.str,
  instagram: EMPTY.str,
  youtube: EMPTY.str,
  tiktok: EMPTY.str,
};
